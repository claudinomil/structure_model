<button
    type="{{ $type }}"
    {{ $attributes }}
    class="{{ $class }}"
    data-bs-toggle="{{ $dataBsToggle }}"
    data-bs-placement="{{ $dataBsPlacement }}"
    title="{{ $title }}">
    <i class="{{ $imageAwesome }}"></i>
    {{ $label }}
</button>
